#!/usr/bin/env bash
# OfferFlow sessionStart hook — injects a daily logging reminder for Cursor.

printf '{\n  "additional_context": "OfferFlow reminder: log any new job applications or interview debriefs in OfferFlow today (http://localhost:8080)."\n}\n'
